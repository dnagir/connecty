-@title = @suggestion.content_brief
= semantic_form_for [@suggestion.project, @suggestion] do |f|
  = f.inputs do
    = f.input :content
    = f.input :status, :collection => Suggestion.statuses_readable
  = f.buttons
