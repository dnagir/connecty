# Sample localization file for English. Add more files in this directory for other locales.
# See http://github.com/svenfuchs/rails-i18n/tree/master/rails%2Flocale for starting points.

en:
  formtastic:
    labels:
      project:
        show_name: "Show project name?"
      suggestion:
        content: "I suggest to"    
    hints:
      project:
        prompt: "short message for a user near the suggestion box (can be blank)"
      suggestion:
        content: "your feedback"
      field_definition:
        name: 'examples: url, screen_size, page_size'
        value: 'JavaScript to calculate the value. For example: window.location'
    actions:
      suggestion:
        create: "Submit"
